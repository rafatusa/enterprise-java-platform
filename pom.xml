<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- The parent manages versions for the whole Spring dependency set, so
       bumping it is the single most effective remediation when the OWASP gate
       reports CVEs in transitive dependencies. Prefer raising this over adding
       suppressions.

       History: 3.3.5 -> 3.3.13 -> 3.5.16. The 3.3.13 bump did NOT clear the
       gate because 3.3.x pins tomcat-embed-core 10.1.42, which carries a set of
       CVSS 9.x authentication- and security-constraint-bypass CVEs. Those are
       directly relevant here: this app is a Tomcat-hosted, authenticated REST
       API, so a DIGEST/FORM auth bypass or a security-constraint bypass is
       exactly the threat model this service has. -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.16</version>
    <relativePath/>
  </parent>

  <groupId>com.example</groupId>
  <artifactId>enterprise-java-platform</artifactId>
  <version>1.0.0</version>
  <name>enterprise-java-platform</name>
  <description>Enterprise Spring Boot platform with full quality and security gates</description>

  <properties>
    <java.version>17</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Embedded Tomcat floor. Spring Boot 3.x requires the 10.1.x line
         (Jakarta EE 9+), and honours this property when resolving tomcat-embed-*.

         KNOWN OPEN ISSUE — do not "clean this up" without reading this note.
         With an authenticated NVD feed the dependency gate printed the matched
         CPE ranges. They are REAL matches, not CPE artifacts:
           CVE-2026-65905 (9.8 DIGEST auth bypass)  >=10.1.0  <10.1.58
           CVE-2026-65637 (9.8 improper input val.) >=10.1.53 <10.1.58
           CVE-2026-68525 (9.1 FORM authz bypass)   >=10.1.0  <10.1.58
           CVE-2026-65182 (9.1 constraint bypass)   >=10.1.0  <10.1.58
           CVE-2026-59084 / 59083 / 55276 / 53434   <=10.1.56 / <10.1.56

         10.1.57 is set here because it is the highest version PUBLISHED TO
         MAVEN CENTRAL, and it genuinely CLEARS the second group (the
         <=10.1.56 / <10.1.56 CVEs). Those are FIXED by upgrade, not suppressed.

         The four <10.1.58 CVEs need 10.1.58, which is TAGGED IN GIT BUT NOT
         PUBLISHED TO MAVEN CENTRAL — setting it fails the build with
         "Could not find artifact org.apache.tomcat.embed:tomcat-embed-core:
         jar:10.1.58 in central". A git tag is cut before the artifacts are
         staged and released; always verify the registry the BUILD resolves
         from, not the source repo. Those four carry time-boxed suppressions in
         config/owasp/suppressions.xml on "no fixed release published" grounds.

         ACTION WHEN 10.1.58 (or later) LANDS ON CENTRAL: raise this property
         and DELETE the four Tomcat entries from suppressions.xml.

         This is a FLOOR, not a pin to hold the parent back: if a future parent
         manages a NEWER Tomcat, raise or remove this property rather than
         freezing the version. -->
    <tomcat.version>10.1.57</tomcat.version>

    <!-- Quality gate thresholds. Single source of truth: raising or lowering a
         gate happens HERE, deliberately, in review — never in a CI step. -->
    <jacoco.line.coverage>0.90</jacoco.line.coverage>
    <jacoco.branch.coverage>0.85</jacoco.branch.coverage>
    <pit.mutation.threshold>70</pit.mutation.threshold>
    <owasp.fail.cvss>7</owasp.fail.cvss>

    <!-- Plugin versions.

         pitest + pitest-junit5-plugin MUST be upgraded as a PAIR and must be
         new enough for the JUnit Platform the parent manages. PIT 1.17.0 with
         junit5-plugin 1.2.1 aborted before mutating on Spring Boot 3.5.x —
         it exited non-zero with no statistics block, which reads exactly like
         a failing score unless the script distinguishes the two (mutation.sh
         does). If PIT ever stops producing a statistics block after a parent
         bump, check this pair FIRST; do not touch pit.mutation.threshold. -->
    <jacoco.version>0.8.12</jacoco.version>
    <pitest.version>1.25.9</pitest.version>
    <pitest.junit5.version>1.2.2</pitest.junit5.version>
    <spotless.version>2.43.0</spotless.version>
    <checkstyle.plugin.version>3.5.0</checkstyle.plugin.version>
    <checkstyle.version>10.18.2</checkstyle.version>
    <pmd.version>3.25.0</pmd.version>
    <spotbugs.version>4.8.6.4</spotbugs.version>
    <dependency.check.version>10.0.4</dependency.check.version>
    <cyclonedx.version>2.9.0</cyclonedx.version>
    <sonar.version>4.0.0.4121</sonar.version>
    <rest.assured.version>5.5.0</rest.assured.version>
    <pact.version>4.6.14</pact.version>
    <springdoc.version>2.8.6</springdoc.version>
    <jjwt.version>0.12.6</jjwt.version>

    <!-- Sonar reads the aggregated JaCoCo XML so unit + IT both count. -->
    <sonar.coverage.jacoco.xmlReportPaths>${project.build.directory}/site/jacoco/jacoco.xml</sonar.coverage.jacoco.xmlReportPaths>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc.version}</version>
    </dependency>

    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- Test scope -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.rest-assured</groupId>
      <artifactId>rest-assured</artifactId>
      <version>${rest.assured.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>au.com.dius.pact.provider</groupId>
      <artifactId>junit5spring</artifactId>
      <version>${pact.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>app</finalName>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>

      <!-- Unit tests: *Test. Integration tests are handled by failsafe. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <includes>
            <include>**/*Test.java</include>
          </includes>
          <excludes>
            <exclude>**/*IT.java</exclude>
            <exclude>**/*PactTest.java</exclude>
          </excludes>
        </configuration>
      </plugin>

      <!-- Integration tests: *IT (Spring Boot IT + REST Assured). -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <configuration>
          <includes>
            <include>**/*IT.java</include>
          </includes>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- COVERAGE GATE: line 90% / branch 85%. jacoco:check FAILS the build. -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>prepare-agent-integration</id>
            <goals>
              <goal>prepare-agent-integration</goal>
            </goals>
          </execution>
          <execution>
            <id>merge-results</id>
            <phase>post-integration-test</phase>
            <goals>
              <goal>merge</goal>
            </goals>
            <configuration>
              <fileSets>
                <fileSet>
                  <directory>${project.build.directory}</directory>
                  <includes>
                    <include>*.exec</include>
                  </includes>
                </fileSet>
              </fileSets>
              <destFile>${project.build.directory}/jacoco.exec</destFile>
            </configuration>
          </execution>
          <execution>
            <id>report</id>
            <phase>verify</phase>
            <goals>
              <goal>report</goal>
            </goals>
            <configuration>
              <dataFile>${project.build.directory}/jacoco.exec</dataFile>
            </configuration>
          </execution>
        </executions>
        <configuration>
          <excludes>
            <exclude>**/Application.class</exclude>
            <exclude>**/config/**</exclude>
            <exclude>**/dto/**</exclude>
            <exclude>**/entity/**</exclude>
          </excludes>
          <rules>
            <rule>
              <element>BUNDLE</element>
              <limits>
                <limit>
                  <counter>LINE</counter>
                  <value>COVEREDRATIO</value>
                  <minimum>${jacoco.line.coverage}</minimum>
                </limit>
                <limit>
                  <counter>BRANCH</counter>
                  <value>COVEREDRATIO</value>
                  <minimum>${jacoco.branch.coverage}</minimum>
                </limit>
              </limits>
            </rule>
          </rules>
        </configuration>
      </plugin>

      <!-- MUTATION GATE: build fails below 70% mutation score. -->
      <plugin>
        <groupId>org.pitest</groupId>
        <artifactId>pitest-maven</artifactId>
        <version>${pitest.version}</version>
        <configuration>
          <targetClasses>
            <param>com.example.app.service.*</param>
            <param>com.example.app.security.*</param>
          </targetClasses>
          <targetTests>
            <param>com.example.app.*Test</param>
          </targetTests>
          <mutationThreshold>${pit.mutation.threshold}</mutationThreshold>
          <outputFormats>
            <outputFormat>HTML</outputFormat>
            <outputFormat>XML</outputFormat>
          </outputFormats>
          <timestampedReports>false</timestampedReports>
        </configuration>
        <dependencies>
          <dependency>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-junit5-plugin</artifactId>
            <version>${pitest.junit5.version}</version>
          </dependency>
        </dependencies>
      </plugin>

      <!-- FORMATTING GATE: spotless:check fails on unformatted code. -->
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless.version}</version>
        <configuration>
          <java>
            <googleJavaFormat>
              <version>1.19.2</version>
              <style>GOOGLE</style>
            </googleJavaFormat>
            <removeUnusedImports/>
            <trimTrailingWhitespace/>
            <endWithNewline/>
          </java>
        </configuration>
      </plugin>

      <!-- CHECKSTYLE GATE: any violation fails. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <version>${checkstyle.plugin.version}</version>
        <configuration>
          <configLocation>config/checkstyle/checkstyle.xml</configLocation>
          <consoleOutput>true</consoleOutput>
          <failOnViolation>true</failOnViolation>
          <violationSeverity>warning</violationSeverity>
          <includeTestSourceDirectory>false</includeTestSourceDirectory>
        </configuration>
        <dependencies>
          <dependency>
            <groupId>com.puppycrawl.tools</groupId>
            <artifactId>checkstyle</artifactId>
            <version>${checkstyle.version}</version>
          </dependency>
        </dependencies>
      </plugin>

      <!-- PMD GATE: priority 1-3 violations fail; CPD catches duplication. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-pmd-plugin</artifactId>
        <version>${pmd.version}</version>
        <configuration>
          <rulesets>
            <ruleset>config/pmd/ruleset.xml</ruleset>
          </rulesets>
          <failOnViolation>true</failOnViolation>
          <failurePriority>3</failurePriority>
          <printFailingErrors>true</printFailingErrors>
          <minimumTokens>100</minimumTokens>
          <includeTests>false</includeTests>
        </configuration>
      </plugin>

      <!-- SPOTBUGS GATE: High priority issues fail the build. -->
      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>${spotbugs.version}</version>
        <configuration>
          <effort>Max</effort>
          <threshold>High</threshold>
          <failOnError>true</failOnError>
          <xmlOutput>true</xmlOutput>
          <excludeFilterFile>config/spotbugs/exclude.xml</excludeFilterFile>
        </configuration>
      </plugin>

      <!-- OWASP DEPENDENCY CHECK: CVSS >= 7 fails the build.

           suppressions.xml holds ONLY documented, time-boxed entries. Every
           suppression must name the CVE, say why it does not apply here, and
           carry an expiry date — an undated suppression is a permanently
           disabled gate. Never raise failBuildOnCVSS to clear a finding. -->
      <plugin>
        <groupId>org.owasp</groupId>
        <artifactId>dependency-check-maven</artifactId>
        <version>${dependency.check.version}</version>
        <configuration>
          <failBuildOnCVSS>${owasp.fail.cvss}</failBuildOnCVSS>
          <formats>
            <format>HTML</format>
            <format>JSON</format>
          </formats>
          <nvdApiDelay>4000</nvdApiDelay>
          <assemblyAnalyzerEnabled>false</assemblyAnalyzerEnabled>
          <suppressionFiles>
            <suppressionFile>config/owasp/suppressions.xml</suppressionFile>
          </suppressionFiles>
        </configuration>
      </plugin>

      <!-- SBOM in CycloneDX format (JSON + XML). -->
      <plugin>
        <groupId>org.cyclonedx</groupId>
        <artifactId>cyclonedx-maven-plugin</artifactId>
        <version>${cyclonedx.version}</version>
        <configuration>
          <outputFormat>all</outputFormat>
          <outputName>bom</outputName>
          <schemaVersion>1.5</schemaVersion>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.sonarsource.scanner.maven</groupId>
        <artifactId>sonar-maven-plugin</artifactId>
        <version>${sonar.version}</version>
      </plugin>
    </plugins>
  </build>
</project>
